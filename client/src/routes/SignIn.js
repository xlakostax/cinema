import SignInForm from "../components/SignInForm";

import {Main} from "../styles/styled";

const Signin = () => {
  return (
    <Main>
      <SignInForm />
    </Main>
  );
}

export default Signin;
